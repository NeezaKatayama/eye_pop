# Eye Health HUD

PC カメラで目の状態を観測し、`疲労`, `眠気リスク`, `ドライアイ傾向`, `集中安定性` をウェルネス用途で推定するローカル Web アプリです。

## 起動

このディレクトリで静的サーバーを起動してください。

```bash
python3 -m http.server 8080
```

その後、ブラウザで `http://localhost:8080` を開きます。

## 注意

- 医療診断用ではありません
- カメラ権限が必要です
- MediaPipe の CDN 読み込みのため、初回はネット接続が必要です
